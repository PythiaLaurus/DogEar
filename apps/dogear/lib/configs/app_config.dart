class AppConfig {
  static const String appName = "DogEar";
  static const String iconPath = "assets/system/app_icon.ico";

  // Args
  /// Argument to indicate that the app should run in silent mode (without showing the main window)
  static const String argSilent = "--silent";
}
